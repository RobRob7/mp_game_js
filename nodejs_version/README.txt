Make sure you're inside the directory after extracting my submission.

Before you run server:
1.) Type "npm init -y"
2.) Type "npm install express"
3.) Type "npm install socket.io"

To run server:
1.) Type "node server.js"

Now in your browser navigate to http://localhost:3000 in two separate windows to play Connect 4 over local multiplayer.
